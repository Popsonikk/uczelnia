extends layout

block content
  h1= title

  if user_list.length
    br
    ul
      each user in user_list
        li= user.first_name + ' ' +(user.username) + ' '+ user.last_name
  else
    p There are no users.