extends layout

block content
  h1= title
  if gallery_list.length
   each gallery in gallery_list
       ul
       li #{gallery.name}; #{gallery.description}; Właściciel: #{gallery.user.username}
   else
      p There are no any galleries.